---
import ServiceCard from '../Cards/Service.astro';
import Container from '../shared/Container.astro';
import Paragraph from '../shared/Paragraph.astro';
import Title from '../shared/Title.astro';

import { MockApi } from '../../lib/mockApi';


const services = await MockApi.get()

type ServicesProps = {
  title: string;
  description: string;
  icon: string;
};
---

<section id="services">
  <Container className="space-y-10 md:sapce-y-12">
    <div class="text-center max-w-3xl mx-auto space-y-4">
      <Title>Nuestros servicios</Title>
      <Paragraph>Como su herramienta digital, podra acceder a los siguientes servicios ofertados</Paragraph>
    </div>
    <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8">
      {
        services.map((service: ServicesProps) => {
          return (
            <ServiceCard
              title={service.title}
              description={service.description}
              icon={service.icon}></ServiceCard>
          );
        })
      }
    </div>
  </Container>
</section>
